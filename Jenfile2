pipeline {
    agent any
    stages {
        stage ("Build") {
            steps {
			    sh "mvn -version"
				sh ' mvn spring-boot:run'
				junit '**/target/surefire-reports/TEST-*.xml'
			}	
        }
    }
	post {
	  always {
	    cleanWs()
	  }
	}
}
