pipeline {
    agent any
    stages {
        stage ("Build") {
            steps {
			    sh "mvn -version"
				build './mvnw package'
				junit '**/target/surefire-reports/TEST-*.xml'
			}	
        }
    }
	post {
	  always {
	    cleanWs()
	  }
	}
}
