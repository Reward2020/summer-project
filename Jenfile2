pipeline {
    agent any
    stages {
        stage ("Build") {
            steps {
			    sh "mvn -version"
				build ' mvn clean package'
				junit '**/target/surefire-reports/TEST-*.xml'
			}	
        }
    }
	post {
	  always {
	    cleanWs()
	  }
	}
}
