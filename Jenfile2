pipeline {
    agent {
	  docker 'maven:3.5-alpine'
	
	}
    stages {
        stage ("Build") {
            steps {
			    sh "mvn -version"
				sh './mvnw clean package:run'
				junit '**/target/surefire-reports/TEST-*.xml'
			}	
        }
    }
	post {
	  always {
	    cleanWs()
	  }
	}
}
