pipeline {
    agent {
	  docker 'maven:3.6-jdk-8'
	
	}
    stages {
        stage ("Build") {
            steps {
			    sh "mvn -version"
				sh ' mvn spring-boot:run'
				junit '**/target/surefire-reports/TEST-*.xml'
			}	
        }
    }
	post {
	  always {
	    cleanWs()
	  }
	}
}
