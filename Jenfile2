pipeline {
    agent any
    stages {
        stage ("Build") {
            steps {
			    sh "mvn -version"
				sh ' mvn clean package'
				junit '**/target/surefire-reports/TEST-*.xml'
			}	
        }
    }
	post {
	  always {
	    cleanWs()
	  }
	}
}
