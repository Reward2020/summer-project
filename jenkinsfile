pipeline {
    agent {
	  docker{
	  image "maven:3.8.1-jdk-8"
	  label "docker"
	  }
	
	}
    stages {
        stage ("Build") {
            steps {
			    sh "mvn -version"
                sh "./mvnw package"
            }
        }
    }
}
